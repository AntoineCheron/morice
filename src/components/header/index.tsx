import React, { SFC } from 'react';

import { Layout } from 'antd';
const { Header } = Layout;

const AppHeader: SFC = () => (
  <Header>
    <h4>This is the header</h4>
  </Header>
)

export default AppHeader;